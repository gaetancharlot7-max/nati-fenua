# Test Credentials

## Backend Admin
- Email: `admin@natifenua.pf` (in `/app/backend/.env` ADMIN_EMAIL)
- Password: see `/app/backend/.env` ADMIN_PASSWORD

## Demo / Test User (auto-created via /api/auth/register)
- Any new test user can be created on the fly with `POST /api/auth/register`
- Sample test session created during testing: see backend logs for latest token

## RSS / Bot Accounts (no login - auto-created)
- `tahiti_infos`, `radio1_tahiti_podcast`, `gnews_tahiti`, etc.
